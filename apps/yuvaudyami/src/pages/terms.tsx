import React from "react";
import Blog from "../components/back/terms/Blog";
import Hero from "../components/back/terms/Hero";

type Props = {};

const terms = (props: Props) => {
  return (
    <div>
      <Hero />
      <Blog />
    </div>
  );
};

export default terms;
