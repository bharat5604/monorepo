import React from "react";
import Blog from "../components/back/privacy/Blog";
import Hero from "../components/back/privacy/hero";

type Props = {};

const privacy = (props: Props) => {
  return (
    <div>
      <Hero />
      <Blog />
    </div>
  );
};

export default privacy;
